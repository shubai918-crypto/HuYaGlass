name: live_core
description: 虎牙直播核心逻辑库
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  crypto: ^3.0.3
  convert: ^3.1.1

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^5.0.0
